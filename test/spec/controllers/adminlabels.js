'use strict';

describe('Controller: AdminlabelsctrlCtrl', function () {

  // load the controller's module
  beforeEach(module('tracklistmeApp'));

  var AdminlabelsctrlCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    AdminlabelsctrlCtrl = $controller('AdminlabelsctrlCtrl', {
      $scope: scope
    });
  }));

  
});
